# dltへの貢献

**dlt**への貢献をご検討いただきありがとうございます！dltの改善にご協力いただき、誠にありがとうございます。このドキュメントでは、プロジェクトへの貢献プロセスをご案内いたします。

## 目次

1. [はじめに](#intro)
2. [変更の送信](#submitting-changes)
3. [コア依存関係の追加または更新](#adding-or-updating-core-dependencies)
4. [Lint](#linting)
5. [テスト](#testing)
6. [ローカル開発](#local-development)
7. [公開（メンテナーのみ）](#publishing-maintainers-only)
8. [リソース](#resources)

## 始める前に

- **大幅な変更や機能強化の提案**: 大幅な変更を検討している場合は、必ず事前に[issueを送信](https://github.com/dlt-hub/dlt/issues/new/choose)してください。これにより、プロジェクトの方向性に沿った作業となり、マージされない可能性のある機能に時間を費やすことなく済みます。注意:
   - 📣 **新しい出力先は、メンテナンスコストが高いため、マージされる可能性は低いです** (ただし、SQLAlchemyの出力先をより多くの方言に対応できるように改善することは喜んで行います)
   - 大幅な変更にはテストとドキュメントが必要であり、多くの場合、テストの作成はコードの作成よりも手間がかかります
   - おそらく[issue](https://github.com/dlt-hub/dlt/issues)が存在します。もしそうであれば、お気軽に実装してください

- **小さな改善**: テスト済みでドキュメント化されている改善であれば、大変嬉しく思います
   - 宛先の認証方法の追加、最適化、追加オプション
   - 使い勝手の向上 - ログメッセージの改善、例外処理の改善、動作の一貫性のなさの修正
  

- **バグの修正**:
   - 既存の問題を確認するには、[open issues](https://github.com/dlt-hub/dlt/issues) を検索し、発見したバグが既に報告されているかどうかを確認してください。
      - 報告されていない場合は、[新しい問題を作成](https://github.com/dlt-hub/dlt/issues/new/choose) してください。ぜひ修正して、解決策を記載したプルリクエストを送信してください。ありがとうございます！
      - バグが既に報告されている場合は、その問題に修正作業中であることをコメントで残してください。これにより、全員が最新情報を把握し、作業の重複を防ぐことができます。

## はじめに <a id="intro"></a>

始めるには、以下の手順に従ってください。

1. `dlt` リポジトリをフォークし、ローカルマシンにクローンします。
2. `make install-poetry` で `poetry` をインストールします（または [公式の手順](https://python-poetry.org/docs/#installation) に従ってください）。
3. `make dev` を実行して、開発環境のものも含め、すべての依存関係をインストールします。
4. `poetry shell` を実行して、`poetry` シェルで作業を開始します。

## 変更の送信 <a id="submitting-changes"></a>

貢献する準備ができたら、以下の手順に従ってください。

1. 実装したい機能、バグ修正、または改善内容を説明する Issue を作成します。
2. フォークしたリポジトリに変更用の新しいブランチを作成します。
3. コードとテストを記述します。
4. `make lint` を実行してコードを lint し、`make test-common` で共通モジュールをテストします。
5. 送信先コードで作業している場合は、テスト送信先へのアクセスを得るためにお問い合わせください。
6. メインリポジトリの **devel** ブランチを対象とするプルリクエストを作成します。

**注:** 特殊なケースでは、このリポジトリ（フォークではなく）にブランチを作成するには、お問い合わせいただく必要があります。詳細は以下をご覧ください。

### アクティブブランチ

`dlt` の次期リリースの準備には、**devel** (GitHub のデフォルトブランチ) を使用しています。通常のコントリビューション（ほとんどのバグ修正を含む）はすべてこのブランチで受け付けています。

通常のスケジュールとは別にリリースする必要があるホットフィックス（ドキュメントを含む）には、**master** ブランチを使用しています。

リリース当日には、**devel** ブランチが **master** にマージされます。`dlt` のすべてのリリースは、**master** からのみ行われます。

### ブランチの命名ルール

Git の履歴では、どのブランチまたは PR で何が変更されたかを人間が読める形式で説明できるようにしたいと考えています。そのため、以下のブランチ命名パターンを使用します（すべて小文字とダッシュを使用し、アンダースコアは使用しません）。

```sh
{category}/{ticket-id}-description-of-the-branch
# example:
feat/4922-add-avro-support
```

#### ブランチのカテゴリ

* **feat** - 実装中の新機能（チケット提出必須）
* **fix** - バグを修正する変更（チケット提出必須）
* **exp** - 新しいアイデアをテストしたり、チームに何かをデモンストレーションしたりするための実験（後日「feat」になる可能性あり）（チケット提出推奨）
* **test** - テストに関連するもの（チケット提出推奨）
* **docs** - ドキュメントの変更（チケット提出任意）
* **keep** - 後で再確認するために保存しておきたいブランチ（チケット提出推奨）

#### チケット番号

ブランチをチケットに添付することをお勧めします。チケットがない場合は作成し、作業内容を説明してください。`feat` および `fix` ブランチの場合はチケットの提出が必須です。`exp` および `test` ブランチの場合はチケットの提出が推奨されます。`docs` ブランチの場合はチケットの提出は任意です。

### ホットフィックスの提出
重大なバグを修正し、`dlt` を予定外にリリースします。通常の手順に従いますが、PR は **master** ブランチに対して行ってください。その場合は Slack でご連絡ください。

### Github Actions を使ったテスト
CI でフォークからのコントリビューションのテストを実行できるようにしました。すべてのテストは実行されますが、認証情報の関係ですべての送信先が利用できるわけではありません。現在、フォークで利用できるのは `duckdb` と `postgres` のみです。

## 完全な CI 認証情報を必要とする変更の送信

新しい送信先を送信したり、認証情報を必要とする送信先（BigQuery、Snowflake、バケットなど）に変更を加えたりする場合は、**コントリビューターとして追加できるよう、弊社までご連絡ください**。その後、`dlt` リポジトリに直接 PR を送信してください。

## コア依存関係の追加または更新 <a id="adding-or-updating-core-dependencies"></a>

私たちの目標は、あらゆる環境において dlt の安定性と互換性を維持することです。
これらのガイドラインに従うことで、dlt の安全性、信頼性、互換性を維持できます。
依存関係の更新を提案する際は、以下の点にご注意ください。

### 更新ガイドライン

1. **重大なセキュリティまたはシステム整合性アップデートのみ:**
   依存関係のメジャーバージョンまたはマイナーバージョンアップデートは、システムの整合性に影響を与える重大なセキュリティ脆弱性または問題が存在する場合にのみ検討してください。このような場合、システムとそれが処理するデータを保護するためにはアップデートが必要です。

2. **「>=」演算子の使用:**
   依存関係を指定する際は、必ず「>=」演算子を使用し、かつ最小バージョンを維持してください。このアプローチにより、プロジェクトは古いシステムや設定との互換性を維持し、解決不可能な依存関係の競合の可能性を軽減できます。

例えば、プロジェクトで現在パッケージ「example-package==1.2.3」を使用しており、セキュリティアップデートが「1.2.4」としてリリースされた場合、「example-package==1.2.4」にアップデートする代わりに、「example-package>=1.2.3,<2.0.0」に設定できます。これにより、必要なセキュリティアップデートが許可されると同時に、将来的に互換性のない可能性のあるメジャーバージョンアップデートへの自動ジャンプが防止されます。
可能なバージョンの最小値を使用する際のもう1つの重要な注意点は、パッケージバージョンが解決できない可能性のあるケースを防ぐことです。

## リンティング <a id="linting"></a>

`dlt` は、リンティング用のプラグインとして `mypy` と `flake8` を使用します。

## テスト <a id="testing"></a>

dlt はテストに `pytest` を使用します。

### 共通コンポーネント

共通コンポーネント（外部リソースを必要としない）をテストするには、`make test-common` を実行してください。

### ローカルの宛先

ローカルの宛先（`duckdb` と `postgres`）をテストするには、`make test-load-local` を実行してください。

### 外部出力先

外部出力先をテストするには、`make test` を使用します。以下の外部リソースが必要です。

1. `BigQuery` プロジェクト
2. `Redshift` クラスタ
3. `Postgres` インスタンス。Docker Compose for Postgres インスタンスは [こちら](tests/load/postgres/docker-compose.yml) にあります。実行すると、インスタンスがテストを実行するように構成されます。

```shell
cd tests/load/postgres/
docker-compose up --build -d
```

必要な環境変数とテスト実行用のコマンドライン例については、`tests/.example.env` を参照してください。それから、`tests/.env` を作成してください。テストの設定は、DLT パイプラインの設定と同じです。
ローカルでテストを行う場合は、上記のリソースへのアクセスを提供します。

## ローカル開発 <a id="local-development"></a>

開発には Python 3.9 を使用してください。これは `dlt` でサポートされている最低バージョンです。`distutils` と `venv` が必要です。[poetry](https://python-poetry.org/docs/managing-environments/) で推奨されている `pyenv` も使用できます。

## 公開（メンテナーのみ）<a id="publishing-maintainers-only"></a>

このセクションは、プロジェクトのバージョン管理と新しいリリースの公開に必要な権限を持つプロジェクトメンテナーを対象としています。コントリビューターの方は、このセクションをスキップしてください。

まず、[ライブラリのバージョン管理方法](README.md#依存関係として追加)をお読みください。

現在のバージョンの信頼できる情報源は `pyproject.toml` であり、`poetry` を使用して管理しています。

### 通常リリース

新しいリリースを公開する前に、プロジェクトのバージョンを適切に上げてください。

1. **devel** ブランチをチェックアウトします。
2. `poetry version patch` を使用して **patch** のバージョンを上げます。
3. `make build-library` を実行して、プロジェクトに変更を適用します。
4. 新しいブランチを作成し、PR を **devel** に送信します。標準のプロセスに従ってマージします。
5. `devel` から `master` へのマージ PR を作成し、マージコミットでマージします。

### ホットフィックスリリース
1. **master** ブランチをチェックアウトします。
2. `poetry version patch` を使用して **patch** のバージョンを上げます。
3. `make build-library` を実行して、プロジェクトに変更を適用します。
4. 新しいブランチを作成し、PR を **master** に送信してマージします。

### プレリリース
場合によっては、**ブランチ**から直接アルファ版をリリースすることがあります。
1. **devel** ブランチをチェックアウトします。
2. `poetry version prerelease` を使用して **alpha** バージョンを上げます。
3. `make build-library` を実行して、プロジェクトに変更を適用します。
4. 新しいブランチを作成し、PR を **devel** に送信してマージします。

### PyPI への公開

バージョンが上がったら、以下の手順に従って新しいリリースを PyPI に公開します。

1. **master** ブランチにいて、CI のすべてのテストに合格した最新のコードがあることを確認します。
2. `poetry version` で現在のバージョンを確認します。
3. PyPI アクセストークンを取得し、`poetry config pypi-token.pypi your-api-token` で設定します。
4. `make publish-library` を実行して新しいバージョンを公開します。
5. バージョンと git タグをリリース名として使用して、GitHub でリリースを作成します。

## リソース <a id="resources"></a>

- [dlt ドキュメント](https://dlthub.com/docs)
- [Poetry ドキュメント](https://python-poetry.org/docs/)

ご質問やご不明な点がございましたら、お気軽にお問い合わせください。私たちは、`dlt` への貢献を成功に導くお手伝いをいたします。楽しいコーディングを！
****